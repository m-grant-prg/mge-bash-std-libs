# shellcheck shell=bash
# shellcheck source-path=@bashlibpath@

[ -n "${_MGE_SCRIPT_EXIT_LIB_LOADED:-}" ] && return
_MGE_SCRIPT_EXIT_LIB_LOADED=1

#########################################################################
#									#
#	mge-script-exit-lib.sh is automatically generated,		#
#		please do not modify!					#
#									#
#########################################################################

#########################################################################
#									#
# Author: Copyright (C) 2026  Mark Grant				#
#									#
# Released under the GPLv3 only.					#
# SPDX-License-Identifier: GPL-3.0-only					#
#									#
# Purpose:								#
# This bash library contains functions related to script exit.		#
#									#
# Exit codes used:-							#
# Bash standard Exit Codes:	0 - success				#
#				1 - general failure			#
# User-defined exit code range is 64 - 113				#
#	C/C++ Semi-standard exit codes from sysexits.h range is 64 - 78	#
#		EX_USAGE	64	command line usage error	#
#		EX_DATAERR	65	data format error		#
#		EX_NOINPUT	66	cannot open input		#
#		EX_NOUSER	67	addressee unknown		#
#		EX_NOHOST	68	host name unknown		#
#		EX_UNAVAILABLE	69	service unavailable		#
#		EX_SOFTWARE	70	internal software error		#
#		EX_OSERR	71	system error (e.g., can't fork)	#
#		EX_OSFILE	72	critical OS file missing	#
#		EX_CANTCREAT	73	can't create (user) output file	#
#		EX_IOERR	74	input/output error		#
#		EX_TEMPFAIL	75	temp failure; user is invited	#
#					to retry			#
#		EX_PROTOCOL	76	remote error in protocol	#
#		EX_NOPERM	77	permission denied		#
#		EX_CONFIG	78	configuration error		#
#	User-defined (here) exit codes range 79 - 113:			#
#		None							#
#									#
# Further Info:								#
#									#
#########################################################################


set -o pipefail


#########################
# Script-wide variables #
#########################
readonly _mge_script_exit_lib_version=0.0.1			# script version
readonly _mge_script_exit_lib_packageversion=@pkgversion@	# package version


#############
# Functions #
#############

# Standard function to tidy up and return exit code.
# Scope:	API
# Parameters:-	$1 is the exit code
# Returns:	Never returns, just exits
mge_script_exit_lib_exit()
{
	exit "$1"
}

# Print out the script and package version strings to stdout
# Scope:	API
# Parameters:	None
# Returns:	0 on success, exits, from mge_basic_lib_output(), on error
mge_script_exit_lib_emit_version()
{
	local msg

	msg="mge script exit library script version is"
	msg+=" $_mge_script_exit_lib_version"
	mge_basic_lib_output "$msg"
	msg="mge script exit library package version is"
	msg+=" $_mge_script_exit_lib_packageversion"
	mge_basic_lib_output "$msg"
}

